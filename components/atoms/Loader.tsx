'use client'

import { SyncLoader } from 'react-spinners'

const Loader = () => {
    return (
        <div className="loader">
            <SyncLoader color="gray" />
        </div>
    )
}

export default Loader
